@model Item

<h2>Delete Item</h2>

<h3>Are you sure you want to delete this item?</h3>
<div>
    <h4>Item</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Item Id</dt>
        <dd class="col-sm-10">@Model.ItemId</dd>

        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">Price</dt>
        <dd class="col-sm-10">@Model.Price</dd>

        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>

        <dt class="col-sm-2">Address</dt>
        <dd class="col-sm-10">@Model.Address</dd>

        <dt class="col-sm-2">Phone number</dt>
        <dd class="col-sm-10">@Model.Phone</dd>

        <dt class="col-sm-2">Rooms</dt>
        <dd class="col-sm-10">@Model.Rooms</dd>

        <dt class="col-sm-2">Beds</dt>
        <dd class="col-sm-10">@Model.Beds</dd>

        <dt class="col-sm-2">Guests</dt>
        <dd class="col-sm-10">@Model.Guests</dd>

        <dt class="col-sm-2">Baths</dt>
        <dd class="col-sm-10">@Model.Baths</dd>

        <dt class="col-sm-2">Image URL</dt>
        <dd class="col-sm-10">@Model.ImageUrl</dd>

        <dt class="col-sm-2">Image URL</dt>
        <dd class="col-sm-10">@Model.ImageUrl2</dd>

        <dt class="col-sm-2">Image URL</dt>
        <dd class="col-sm-10">@Model.ImageUrl3</dd>

    </dl>

    <form asp-action="DeleteConfirmed" asp-route-id="@Model.ItemId" method="post">
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Table" class="btn btn-secondary">Cancel</a>
    </form>
</div>